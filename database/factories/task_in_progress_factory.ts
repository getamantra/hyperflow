import factory from '@adonisjs/lucid/factories'
import TaskInProgress from '#models/task_in_progress'

export const TaskInProgressFactory = factory
  .define(TaskInProgress, async ({}) => {
    return {}
  })
  .build()
